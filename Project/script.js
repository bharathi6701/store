// JavaScript for adding items to cart (simple console logging example)
function addToCart(product, price) {
    console.log(${product} added to cart at $${price});
    alert(${product} has been added to your cart!);
}